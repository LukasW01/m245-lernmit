<%= link_to task_path(task), class: "card task #{'done' if task.done?}", tabindex: "0", 'up-layer': 'new' do %>
  <div class="card-body d-flex flex-column">
    <div class="card-text flex-grow-1">
      <b><%= task.title %></b>
    </div>
  </div>
<% end %>