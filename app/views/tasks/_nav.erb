
<ul class="nav nav-tabs" id="tab" role="tablist">
  <li class="nav-item" role="presentation">
    <%= link_to 'Upcoming', filter_tasks_path(status: 'upcoming'), 'up-target': ".container", class: 'nav-link', id: 'pills-home-tab', 'data-bs-toggle': 'pill', 'data-bs-target': '#pills-tab', role: 'tab'%>
  </li>
  <li class="nav-item" role="presentation">
    <%= link_to 'Past due', filter_tasks_path(status: 'past_due'), 'up-target': ".container", class: 'nav-link', id: 'pills-profile-tab', 'data-bs-toggle': 'pill', 'data-bs-target': '#pills-tab', role: 'tab' %>
  </li>
  <li class="nav-item" role="presentation">
    <%= link_to 'Completed', filter_tasks_path(status: 'completed'), 'up-target': ".container", class: 'nav-link', id: 'pills-contact-tab', 'data-bs-toggle': 'pill', 'data-bs-target': '#pills-tab', role: 'tab' %>
  </li>
</ul>


<div class="tasks">
  <% if params[:status].nil? || %w[upcoming completed past_due].include?(params[:status].to_s) %>
    <% if @load_tasks.present? %>
      <% @load_tasks&.each_with_index do |task, i| %>
        <%= render task %>
      <% end %>
    <% else %>
      <div class="alert alert-danger" role="alert">
        No tasks found
      </div>
    <% end %>
  <% else %>
    <div class="alert alert-danger" role="alert">
      Invalid filter
    </div>
  <% end %>
</div>